{
    "name": "soltrain-voting-program",
    "scripts": {
        "compile": "yarn run g:anchor build && yarn run lint-fix",
        "lint": "yarn run g:prettier",
        "lint-fix": "yarn run g:prettier-fix",
        "test": "yarn run g:anchor test --skip-local-validator"
    },
    "dependencies": {
        "@coral-xyz/anchor": "^0.30.0"
    },
    "devDependencies": {
        "@types/chai": "^4.3.16",
        "@types/mocha": "^10.0.6",
        "chai": "^5.1.1",
        "mocha": "^10.4.0",
        "ts-mocha": "^10.0.0"
    }
}
